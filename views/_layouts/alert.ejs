<script src="/socket.io/socket.io.js"></script>
<script>
    const socket = io();
    socket.on("connect", () => {
        console.log(socket.id);
    });
    socket.on("msg", (msg) => {
        console.log(msg);
        M.toast({ html: `${msg.message}<br>-${msg.name}` })
    })
</script>